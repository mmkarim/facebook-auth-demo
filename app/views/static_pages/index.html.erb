<% if current_user %>
  <%= image_tag current_user.image_url, alt: current_user.name %>
  <%= link_to "Log Out", logout_path, method: :delete, class: "btn btn-primary" %>
<% else %>
  <%= link_to "Facbook Login", "/auth/facebook", class: "btn btn-primary" %>
<% end %>
